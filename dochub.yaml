# Метаданные пакета
$package:
  ceaf-changes-en:
    # Идентификатор пакета
    # Краткое название пакета
    name: CEAF Changes English
    # Поставщик
    vendor: villanella
    # Описание
    description: CEAF Change Management En
    # Версия
    version: 1.0.2
    # Зависимости от других пакетов
    dependencies:
      dochub: ">=3.9.0"
    #  ceaf-changes: ">=1.2.6"    # all dependencies between packages should be set in the project.

imports:
  #- _metamodel_/packages.yaml
  - entities/root.yaml
  - architecture/interface/root.yaml
  #- tests.yaml    # uncomment to test metamodel


